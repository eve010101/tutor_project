# 数据安全修复报告

日期：2026-08-17

## 结论

扫描报告中指定的第 1、2、6、10 项数据安全边界已在代码和数据库迁移中修复。数据库迁移尚需在目标 Supabase 项目中执行后，线上数据库才会实际生效。

## 已关闭的路径

1. 未认证的开发重置密码接口不再允许按手机号直接修改任意账号密码；接口现在固定返回不可用，等待短信验证码流程接入。
2. 家长需求详情和匹配联系方式查询不再使用 `service_role` 绕过 RLS；未获授权的会话无法借助页面读取数据。
3. 注册接口不再记录服务密钥前缀、完整内部邮箱或手机号，也不再向客户端返回 Supabase 内部异常、堆栈、用户 ID 和手机号。
4. 管理后台不再依赖开发环境判断；只有 `ADMIN_USER_IDS` 中明确列出的已登录用户可以访问。
5. 家教认证 PDF 存储桶改为私有；家教只能访问自己的文件，审核后台在管理员打开页面时生成三十分钟有效的签名链接，文件本身不会过期。
6. `profiles.role` 和 `profiles.phone` 对普通用户不可修改，阻止角色提升后读取其他角色数据。
7. 普通用户不再读取全部 `tutor_profiles` 基表；家教目录继续通过仅包含审核通过资料、且不含认证文件路径的视图读取。
8. 普通已登录用户不再无差别读取全部家长需求；家长只读自己的需求，家教通过角色策略读取需求。
9. 家教不能把自己的审核状态直接改为 `approved`，认证文件路径必须位于自己的目录。
10. 匹配双方只能修改自己一侧的同意、材料请求和评价字段，不能替对方同意或直接伪造 `matched` 状态来提前解锁手机号。

## 部署要求

1. 先备份 Supabase 数据库。
2. 在 Supabase SQL Editor 执行 `supabase/fix_security_boundaries.sql`。
3. 在部署环境配置 `ADMIN_USER_IDS`，值为管理员的 Supabase Auth 用户 UUID；多个 UUID 用英文逗号分隔。
4. 重新部署应用。
5. 使用家长、家教、审核员三个测试账号验证需求列表、双向匹配、联系方式解锁、资料上传和审核页面。

## 已完成验证

- `git diff --check`：通过。
- `npx.cmd tsc --noEmit`：通过。
- `npm.cmd run build`：通过。
- 静态绕过复核：应用业务页面中仅注册和授权审核后台继续使用管理员客户端；需求详情与联系方式读取已移除管理员客户端。

## 剩余验证限制

当前环境未连接目标 Supabase 数据库，因此无法在真实 RLS 会话中执行家长、家教、审核员三角色的数据库集成测试。迁移必须部署到测试或生产 Supabase 后完成上述角色回归验证。
